import {
  Canvas,
  Meta,
  Title,
  Subtitle,
  Description,
  Primary,
  Controls,
  Stories,
  Source,
  Story,
} from "@storybook/blocks";

import * as ButtonStories from "./Button.stories";

<Meta of={ButtonStories} />

# Button

アプリケーション内で使用するボタンのコンポーネントです。\
具体的なユースケースが決まっているものは、ストーリーに記載されています。

<Primary />

# パラメーター

コンポーネント内で使用できるパラメーターです。\
パラメーターを変更することで上にあるコンポーネントが変化します。

<Controls />

# ストーリー

コンポーネントのユースケースを記載しています。\
既存のコンポーネントを使用する際に参考にしてください。

## 共通

### 保存

アプリケーション内で保存時に使用します。

<Canvas of={ButtonStories.Save} />

### キャンセル

アプリケーション内でキャンセル時にします。

<Canvas of={ButtonStories.Cancel} />

### 無効化

アプリケーション内でボタンを無効化する時に使用します。

<Canvas of={ButtonStories.Disabled} />

## タイムレコーダー

### 勤務開始

タイムレコーダーコンポーネント内で勤務開始時間を打刻する際に使用します。

<Canvas of={ButtonStories.ClockIn} />

### 勤務終了

タイムレコーダーコンポーネント内で勤務終了時間を打刻する際に使用します。

<Canvas of={ButtonStories.ClockOut} />

### 休憩開始

タイムレコーダーコンポーネント内で休憩開始時間を打刻する際に使用します。

<Canvas of={ButtonStories.RestStart} />

### 休憩終了

タイムレコーダーコンポーネント内で休憩終了時間を打刻する際に使用します。

<Canvas of={ButtonStories.RestEnd} />

### 直行

タイムレコーダーコンポーネント内で直行する際に使用します。

<Canvas of={ButtonStories.GoDirectly} />

### 直帰

タイムレコーダーコンポーネント内で直帰する際に使用します。

<Canvas of={ButtonStories.ReturnDirectly} />

## ログイン

アプリケーション内でログインする際に使用します。

<Canvas of={ButtonStories.Login} />

## ログアウト

アプリケーション内でログアウトする際に使用します。

<Canvas of={ButtonStories.Logout} />

## テーブル用

一覧形式のビューで使用するボタンです。

### 編集

テーブル内で編集する際に使用します。

<Canvas of={ButtonStories.EditSmall} />
